#include <stdio.h>
#include <cs50.h>
int main(void)
{
    int counter = 0;
    while (true)
    // for (int i = 0; i<3; i++)
    {
        printf("meow\n");
        printf("counter = %d \n", counter);
        counter ++;
    }



}